# TODO for Code Ecosystem Test Runners

- **`test_conda.py`**:
  - Current: Tests `numpy`.
  - TODO: Add a test case for `pandas`.

- **`test_cran.py`**:
  - Current: Tests `ggplot2`.
  - TODO: Add a test case for `dplyr`.

- **`test_crates.py`**:
  - Current: Tests `serde`.
  - TODO: Add a test case for `rand`.

- **`test_github.py`**:
  - Current: Tests user `torvalds`.
  - TODO: Add a test case for a large organization like `facebook`.

- **`test_github_forked_repos.py`**:
  - Current: Tests users `torvalds`, `gvanrossum`.
  - TODO: Add a test case for another prolific developer like `tj` (TJ Holowaychuk).

- **`test_github_new_features.py`**:
  - Current: Tests user `torvalds` and repo `nodejs/node`.
  - TODO: Add a test case for user `facebook` and repo `facebook/react`.

- **`test_go_proxy.py`**:
  - Current: Tests `github.com/gin-gonic/gin`.
  - TODO: Add a test case for `google.golang.org/grpc`.

- **`test_homebrew.py`**:
  - Current: Tests `python`, `node`, `git`.
  - TODO: Add a test case for `go`.

- **`test_npm.py`**:
  - Current: Tests `react`.
  - TODO: Add a test case for `vue`.

- **`test_nuget.py`**:
  - Current: Tests `Newtonsoft.Json`.
  - TODO: Add a test case for `NUnit`.

- **`test_pypi.py`**:
  - Current: Tests `requests`.
  - TODO: Add a test case for `django`.

- **`test_rubygems.py`**:
  - Current: Tests `rails`.
  - TODO: Add a test case for `jekyll`.
